name: betrix

services:
  backend:
    build: ./backend
    container_name: betrix-backend
    restart: unless-stopped
    env_file: .env
    environment:
      SERVER_PORT: 8080
    ports:
      - "127.0.0.1:${BACKEND_PORT:-8083}:8080"
