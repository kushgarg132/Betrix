# Copy to .env.local for local dev (never commit .env / .env.local).
VITE_SERVER_HOST=http://localhost
VITE_SERVER_PORT=8083
# Google Sign-In: the OAuth 2.0 web client id (Google Cloud Console, not secret — matches the
# backend's GOOGLE_CLIENT_ID in the repo-root .env.example).
VITE_GOOGLE_CLIENT_ID=150680747756-m8lkb467482bveh2osnk2o45fusjr4q5.apps.googleusercontent.com
