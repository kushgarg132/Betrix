services:
  - type: web
    name: betrix-backend
    env: java
    buildCommand: ./gradlew build
    startCommand: java -jar build/libs/backend-0.0.1-SNAPSHOT.jar
    buildFilter:
      paths:
        - backend/**

    envVars:
      - key: JAVA_VERSION
        value: 17

    rootDir: backend
